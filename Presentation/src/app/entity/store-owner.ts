// import { Store } from "./store";

export class StoreOwner {
    constructor(
        public name : string = "",
        public address : string="",
        public isApproved: boolean = false) {
    }
}
